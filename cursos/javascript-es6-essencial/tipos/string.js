const text = "Texto"

console.log(text.length); // Comprimento
console.log(text.split('x')); // Separa usando o caracter x
console.log(text.replace('x', 'xxxx')); // Substitui x por xxxx
console.log(text.slice(-1)); // Último caracter
console.log(text.slice(0,-1)); // Todas menos o último caracter
console.log(text.slice(1)); // Todas menos o primeiro caracter
console.log(text.substring(0, 2)); // Duas primeiras letras
